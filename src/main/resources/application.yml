spring:
  application:
    name: translator
  flyway:
    baseline-on-migrate: true
  datasource:
    driver-class-name: org.postgresql.Driver
#    url: jdbc:postgresql://localhost:10000/translator
    url: jdbc:postgresql://translator_db:5432/translator
    username: translator
    password: translator

translator-api:
    url: "https://ftapi.pythonanywhere.com/"
